[user]
    name = Zahid Kizmaz
    username = zahidkizmaz
    email = tech@zahid.rocks

[core]
    pager = delta

[delta]
    dark = true
    side-by-side = true

[merge]
    # See all options: git mergetool --tool-help
    tool = nvimdiff3
    conflictStyle = diff3

[mergetool]
    keepBackup = false

[color]
    ui = true
    diff = auto
    status = auto
    branch = auto
    interactive = auto

[interactive]
    diffFilter = delta --color-only

[blame]
    date = short
	ignoreRevsFile = .git-blame-ignore-revs
